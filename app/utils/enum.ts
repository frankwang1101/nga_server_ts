export enum UserStatus {
  '启用' = 0,
  '禁用',
  '禁言',
}

export enum PostState {
  '有效' = 0,
  '失效',
  '删除',
}

export enum ReplyType {
  '来自帖子' = 0,
  '来自回复',
}

export enum ReplyState {
  '有效' = 0,
  '删除',
  '折叠',
}
